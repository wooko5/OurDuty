spring:
  datasource:
    url: jdbc:mysql://localhost:3306?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${USER_NAME} #TODO:??? properties ??? ????? ???
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true #System.out ? ?????? ?? SQL? ???, ?? ??? ?? ???? ????? ????
        format_sql: true #logger? ?? ?????? ?? SQL? ???
#        use_sql_comments: true #???? ????

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace